# Database
# Supabase local database URL (update for production)
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:54322/postgres"

# NextAuth Configuration
# The base URL of your application
NEXTAUTH_URL="http://localhost:3000"
# Secret used to encrypt the NextAuth JWT and to encrypt email verification tokens
# Generate a new one with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"

# Email Provider (SMTP) - Required for email authentication
# For local development with Supabase, you can use Inbucket email testing server
# View emails at: http://127.0.0.1:54324 (when Supabase is running)
# For production, configure a real SMTP server
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="your-email@example.com"
SMTP_PASSWORD="your-password"
SMTP_FROM="noreply@example.com"
