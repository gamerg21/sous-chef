# Docker Compose dev/test (build from source)
#
# Copy this file to ".env.docker" (which is gitignored):
#   cp env.docker.example .env.docker
#
# Start Docker Compose using the docker env + isolated project name:
#   docker compose --env-file .env.docker -p souschef_docker up -d --build
#
# NOTE:
# - Use different ports than Supabase CLI + pnpm dev so both can run together.
# - Inside Docker, Postgres is reached at hostname "postgres:5432" (not localhost).

# Postgres container config
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-this-strong-password
POSTGRES_DB=souschef

# Host ports (pick ports that don't conflict with your local dev)
POSTGRES_PORT=5433
APP_PORT=3001

# App runtime DB URL (inside the docker network)
DATABASE_URL=postgresql://postgres:change-this-strong-password@postgres:5432/souschef?schema=public

# NextAuth (match your host-mapped APP_PORT)
NEXTAUTH_SECRET=change-me-use-openssl-rand-base64-32
NEXTAUTH_URL=http://localhost:3001

# Optional email auth
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@souschef.local

# Optional HTTPS mode (useful for iOS Safari barcode scanning)
ENABLE_HTTPS=false


